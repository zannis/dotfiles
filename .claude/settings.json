{
  "permissions": {
    "allow": [
      "Read(~/.claude/CLAUDE.md)",
      "Read(~/.claude/settings.json)",
      "Bash(cargo check:*)",
      "Bash(cargo build:*)",
      "Bash(npm install:*)",
      "Bash(npm run build:*)",
      "Bash(find:*)",
      "Bash(ls:*)",
      "WebFetch(domain:docs.rs)",
      "WebFetch(domain:github.com)",
      "WebFetch(domain:raw.githubusercontent.com)",
      "Skill(superpowers:*)"
    ],
    "deny": [
      "Read(**/.env)",
      "Read(**/.env.*)",
      "Read(./secrets/**)"
    ]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "FILE=$(jq -r '.tool_input.file_path // .tool_response.filePath // empty'); case \"$FILE\" in *.rs) ;; *) exit 0;; esac; DIR=$(dirname \"$FILE\"); while [ \"$DIR\" != \"/\" ] && [ ! -f \"$DIR/Cargo.toml\" ]; do DIR=$(dirname \"$DIR\"); done; [ -f \"$DIR/Cargo.toml\" ] && cd \"$DIR\" && cargo fmt --all -q 2>&1 || true",
            "statusMessage": "cargo fmt --all"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "CMD=$(jq -r '.tool_input.command'); if echo \"$CMD\" | grep -qE '^cargo (check|clippy|test|build|c|t|b)'; then cargo fix --allow-dirty --allow-staged; elif echo \"$CMD\" | grep -qE '^cd .* && cargo (check|clippy|test|build|c|t|b)'; then DIR=$(echo \"$CMD\" | sed -E 's/^cd ([^ ]*) &&.*/\\1/'); if [[ \"$DIR\" = /* ]]; then cd \"$DIR\"; else cd \"$CLAUDE_PROJECT_DIR/$DIR\"; fi && cargo fix --allow-dirty --allow-staged; fi"
          },
          {
            "type": "command",
            "command": "CMD=$(jq -r '.tool_input.command // empty'); echo \"$CMD\" | grep -qE 'git commit($|[[:space:]])' || exit 0; DIR=\"$PWD\"; PREFIX_DIR=$(echo \"$CMD\" | sed -nE 's/^[[:space:]]*cd[[:space:]]+([^ &]+).*/\\1/p'); if [ -n \"$PREFIX_DIR\" ]; then case \"$PREFIX_DIR\" in /*) DIR=\"$PREFIX_DIR\" ;; *) DIR=\"$PWD/$PREFIX_DIR\" ;; esac; fi; while [ \"$DIR\" != \"/\" ] && [ ! -f \"$DIR/Cargo.toml\" ]; do DIR=$(dirname \"$DIR\"); done; [ -f \"$DIR/Cargo.toml\" ] || exit 0; cd \"$DIR\" || exit 0; OUTPUT=$(cargo clippy --workspace --all-targets --all-features -q -- -D warnings 2>&1); EXIT=$?; if [ $EXIT -ne 0 ]; then echo \"cargo clippy failed - fix warnings before committing:\" >&2; echo \"$OUTPUT\" | tail -50 >&2; exit 2; fi",
            "timeout": 300,
            "statusMessage": "cargo clippy (pre-commit gate)"
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "cat ~/.claude/CLAUDE.md"
          }
        ]
      }
    ]
  },
  "enabledPlugins": {
    "figma@claude-plugins-official": true,
    "linear@claude-plugins-official": true,
    "ralph-loop@claude-plugins-official": true,
    "rust-analyzer-lsp@claude-plugins-official": true,
    "superpowers@claude-plugins-official": true,
    "claude-retrospective@bitwarden-marketplace": true,
    "bitwarden-code-review@bitwarden-marketplace": true,
    "gopls-lsp@claude-plugins-official": true
  },
  "extraKnownMarketplaces": {
    "bitwarden-marketplace": {
      "source": {
        "source": "github",
        "repo": "bitwarden/ai-plugins"
      }
    }
  },
  "skipDangerousModePermissionPrompt": true,
  "theme": "dark",
  "autoCompactEnabled": false,
  "skipAutoPermissionPrompt": true,
  "voiceEnabled": true
}
